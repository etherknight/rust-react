language: 'unknown'
type: 'application'

tasks:
  start:
    command: 'dotnet run --project ./Server.Api/Server.Api.csproj'
    platform: system   
  test:
    command: 'dotnet test Server.sln'
    platform: system
  build:
    command: 'dotnet publish Server.sln'
    platform: system