language: 'typescript'
type: 'library'

tasks:
  lint:
    command: 'oxlint -D all src'
    platform: node 
  test:
    command: jest
    platform: node
  build:
    command: 'vite build'
    platform: node